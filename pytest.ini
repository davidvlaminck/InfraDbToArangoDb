[pytest]
testpaths =
    tests
python_files =
    test_*.py
addopts = -ra
markers =
    benchmark: micro-benchmarks that print timing information (not strict performance assertions)
    integration: tests that hit the real API/ArangoDB and may have side effects (run explicitly)
